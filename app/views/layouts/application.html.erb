<!DOCTYPE html>
<html>
  <head>
    <%# Replace with your application name %>
    <title>Hey Chickpea!</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="/stylesheets/application.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  </head>
<body>
    <div navbar class="navbar">
        <a href="/" class="navbar-brand">
            <h1 class="display-4" >The Chickpea Community</h1></a>
        <p class="navbar ml-auto">
      <% @user = User.find_by({"id" => session["user_id"]}) %>
      <% if @user == nil %>
        <a href="/users/new" class="btn btn-warning">Sign Up</a> | 
        <a href="/login" class="btn btn-warning">Login</a>
      <% else %>
      <a> Welcome, <%= @user["username"] %>!| </a><a href="/logout">Logout</a>
      <% end %>
    </p>
        </div>
        <div class ="navbar" >
        <p class="lead" ><em>Chickify your life, join the chicks!<em></p>    
    </div>

    <p>
      <%= flash["notice"] %>
    </p>

    <%= yield %>
    
  </body>
</html>
